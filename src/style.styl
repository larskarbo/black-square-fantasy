body
    background black
    margin 0
    padding 0
    
    #root
        width 100vw
        height 100vh

        svg, canvas
            width 100%
            height 100%
            // animation:spin 45s linear infinite;
        
    
@keyframes spin {
    100% { 
        transform:rotate(360deg);
    }
}